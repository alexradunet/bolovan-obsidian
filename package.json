{
  "name": "nazar-obsidian-plugin",
  "version": "0.1.0",
  "description": "Run pi against your Obsidian vault from inside Obsidian.",
  "type": "module",
  "private": true,
  "main": "main.js",
  "scripts": {
    "build": "tsc --noEmit && node esbuild.config.mjs production",
    "test": "npm run build && vitest run && node tests/load-built-plugin.cjs"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "devDependencies": {
    "@types/node": "22.15.17",
    "esbuild": "0.25.5",
    "obsidian": "latest",
    "typescript": "5.9.3",
    "vitest": "3.2.7"
  }
}
